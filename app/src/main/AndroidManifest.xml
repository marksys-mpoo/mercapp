<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mercapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-library android:name="com.google.android.maps" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- android:debuggable="true" -->
    <!--
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".usuario.gui.TelaMenu"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".usuario.gui.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".usuario.gui.Cadastro"
            android:screenOrientation="portrait" />
        <activity
            android:name=".supermercado.gui.TelaSupermercado"
            android:screenOrientation="portrait" />
        <activity
            android:name=".usuario.gui.TelaMenu"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".usuario.gui.Login"
            android:screenOrientation="portrait" />
        <activity
            android:name=".supermercado.gui.CadastroSupermercados"
            android:screenOrientation="portrait" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!--
     ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".usuario.gui.CadastroPessoa" />
        <activity android:name=".supermercado.gui.ListaSupermercados" />
        <activity android:name=".supermercado.gui.ListaProdutos" />
        <activity android:name=".supermercado.gui.CadastroProdutos" />
        <activity android:name=".supermercado.gui.ListaProdutosDoSupermercado" />
        <activity android:name=".infra.Sobre" />
        <activity android:name=".infra.Administrador" />
        <activity android:name=".usuario.gui.RecuperarSenha" />
        <activity android:name=".supermercado.gui.ListaImagens" />
        <activity android:name=".supermercado.gui.ImagemListAdapter" />
        <activity android:name=".usuario.gui.Ajuda" />
        <activity android:name=".supermercado.gui.Ofertas" />
        <activity android:name=".supermercado.gui.DescricaoProduto"></activity>
    </application>

</manifest>